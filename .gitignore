# Maven build output.
target/

# IDE
.idea/
*.iml
.classpath
.project
.settings/
bin/

# Agent instructions — local, not versioned. The binding written standard is docs/conventions/ and the
# module package-info javadoc, which live next to the code they describe and are reviewed with it.
CLAUDE.md

# OS
.DS_Store
Thumbs.db

# Local env & secrets — never commit
.env
.env.local
*.pem
*.p12
*.keystore

# Dev app config — materialised from a template, may hold real secrets
services/bus-core/src/main/resources/application-dev.yml

# JVM crash logs
hs_err_pid*
replay_pid*
*.log
